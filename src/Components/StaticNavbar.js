import React from 'react'

// Components
import Logo from "./Logo"

function StaticNavbar() {
    return (
        <div className="static-navbar">
            <p>Store made with <Logo /></p>
        </div>
    )
}

export default StaticNavbar
